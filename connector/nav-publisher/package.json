{
  "name": "@gold-custody/nav-publisher",
  "version": "0.1.0",
  "private": true,
  "description": "Publishes XAU oracle price points from configured sources, refusing to publish when sources diverge beyond the configured threshold.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@gold-custody/shared": "*",
    "decimal.js": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
